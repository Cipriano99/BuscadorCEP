import styled from 'styled-components';

export const Container = styled.nav`
  display: flex;
  align-items: flex-end;
  flex-wrap: wrap;
  gap: 24px;

  li {
    list-style: none;
    font-size: 1.2rem;
    h2 {
      font-size: 1.4rem;
    }
  }
`;
